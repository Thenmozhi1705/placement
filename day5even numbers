int main()
{
 int start, end;
    scanf("%d %d",&start,&end);
    int i=start;
    while(i<=end)
    {
        if(i%2 == 0)
        {
            printf("%d ",i);
        }
        i++;
    }
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
