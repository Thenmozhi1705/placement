int main() {
    int a,b,c=0, i;
    scanf("%d %d", &a,&b);
  for(i=0; i<b; i++)
  {
      c= c+a;
  }
      printf("%d",c);
    
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
